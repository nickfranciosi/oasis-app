//- specify layout here
extends ./layouts/layout.jade

//- specifiy page title here
block title
 title More to Youth - Profile

//- page content here
block content

  // Nav
  include /layouts/partials/nav-trans.jade

  div#profile
    section.hero_profile
      .container
        .table
          .row
            div.col-sm-5
              img.img-max(src="/{{ $user['image_path'] }}")
              @if (isset($user) && Auth::check())
              @if ($user->facebook_user_id == Auth::user()->facebook_user_id)
              div.edit-block
                a#delete-user(href='/delete') Remove My Picture
                a.btn-edit.pull-right(href="/picture") Edit
              @endif
              @endif
            div.col-sm-7
              h2.header {{ $user->name }}
              p Oasis encourages people to show their disapproval for teen stereotypes. Help us take a stand by turning your existing Facebook profile picture into your own customized campaign photo.
              include ./layouts/partials/shareButton.jade

    // Instagram feed
    include ./layouts/partials/section-gallery-grid.jade

    // Social band
    include ./layouts/partials/section-social-band.html

    // How you can help
    include ./layouts/partials/section-help.jade
    //- include ./layouts/partials/share.html


