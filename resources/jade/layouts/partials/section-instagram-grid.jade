section.instragram-grid
  .container
    h2.header.text-center Instagram

    .row
    @foreach ($allUsers as $currentUser)
    .col-sm-4
      img.img-max(src="/{{ $currentUser['image_path'] }}")
    @endforeach
    .view-more
        a.btn-main.veiw-more View More