section.gallery-grid
  h2.header.text-center Photo Gallery

  .row
    @foreach ($allUsers as $currentUser)
    .col-xs-6.col-sm-4
      img.img-max(src="/{{ $currentUser['image_path'] }}")
    @endforeach

  .view-more
    a.btn.btn-main(href="/gallery") View More Pictures
    a.btn.btn-main(href="/picture") Create Your Own